前置++ 重载函数返回为引用，主要是避免调用拷贝构造和析构，减少开销，另一方面前置++，返回的是对象本身，不会因为调用完毕就销毁，所以使用引用返回。比较好，但也可以不用。
后置++，重载返回的不是对象本身，先返回临时对象，再对原来的对象自增，调用完临时对象释放，不能用引用返回。

引用返回左值。对+=，（要求操作数为左值的运算符）进行重载，因为+=要求操作数是左值性的，所以要用引用返回。
可以返回对象，但是不能实现连续赋值，改变了=的语义。
